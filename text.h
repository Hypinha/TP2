#pragma once
#include "color.h"
//prototipos das funções

void Alinhado(char[], cores, cores, int);							// Função para alinhar e colorir o texto

void AlinhadoInteiro(int[], cores, cores, int);					// Função para alinhar e colorir inteiros

void AlinhadoFlutuante(double[], cores, cores, int);				// Função para alinhar e colorir ponto-flutuantes

void Linha(char, int, cores, cores);								// Função para criar linhas

void Caps(char[]);																// Função para deixar o texto em maiúsculo